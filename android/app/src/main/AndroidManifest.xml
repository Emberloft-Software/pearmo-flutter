<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Needed for every network call this app makes (Supabase, FCM
         registration) in a release build. Previously only declared in the
         debug/profile manifest variants — found + fixed while wiring up
         push notifications, since a missing INTERNET permission would
         silently break FCM token registration (and everything else network-
         related) in a release APK. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Required on Android 13+ (API 33) for local notification banners
         (flutter_local_notifications) — without this the runtime permission
         request silently has nothing to grant. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Required for the check-in alarm (AndroidScheduleMode.alarmClock) to
         fire at the exact scheduled instant, including while the device is
         idle. On Android 12+ this may still need the user to enable
         "Alarms & reminders" for the app in system settings on some OEMs. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <!-- Lets the check-in alarm attempt to wake/light the screen like a
         real alarm. Android 14+ (API 34) restricts this to alarm/calling
         apps specifically — may silently degrade to a loud heads-up
         notification instead on some devices/OS versions. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <application
        android:label="Pearmo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Channel/icon FCM falls back to for a background/terminated push
             notification, when there's no Dart code running to pick one —
             same channel id `NotificationService` already uses for
             foreground notifications, so both paths look identical. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="pearmo_default" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
